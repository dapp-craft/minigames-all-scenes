export const modelPath = new Map([
    ['defaultCat', "/"],
    ['test', '']
])

export const entityAmount = 20

export const entityConfig = {
    distance: 5,
    initialEntitySize: .1,
    spacing: .2,
    maxRowLength: 1,
}